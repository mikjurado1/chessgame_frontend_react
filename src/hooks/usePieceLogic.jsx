// usePieceLogic.js
import { useState, useEffect } from "react";

const usePieceLogic = (pieceType, pieceColor, newIndex) => {
  // Aquí puedes usar useState, useEffect y otra lógica

  // Retorna cualquier valor o función que necesites
  return {
    // ... valores o funciones
  };
};

export default usePieceLogic;
